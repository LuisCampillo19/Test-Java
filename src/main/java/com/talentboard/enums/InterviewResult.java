package com.talentboard.enums;

public enum InterviewResult {
    PENDING,  // Interview scheduled but not yet held / no result recorded
    PASSED,
    FAILED,
    NO_SHOW   // Candidate did not attend
}
